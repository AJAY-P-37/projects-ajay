{
  "name": "common-types",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "clean": "rm -rf node_modules dist"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
